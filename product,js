var quantity = 0;
document.getElementById("quantity-display").innerHTML=+quantity
function addToCart() {
     alert("Item has been added to your cart!");
    }


function increase() {
     quantity++; 
     document.getElementById("quantity-display").innerHTML=+quantity
 }
function dcrease() {

     if (quantity > 0) {
        quantity--;
    }
    else {
        alert("Cannot decrease quantity below 0!");}
     document.getElementById("quantity-display").innerHTML=+quantity

 }